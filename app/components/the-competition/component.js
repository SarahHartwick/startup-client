import Ember from 'ember';

export default Ember.Component.extend({
  // competitors: Ember.computed('idea.competitors', function () {
  //   this.get('store').findAll('competitor')
  //   .then((results) => {
  //       return results.toArray().filter((result) => {
  //         if(result.get('idea') === this.get('idea')) {
  //           return result;
  //         }
  //       });
  //   });
  // }),
});
